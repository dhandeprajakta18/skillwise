/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "images.unsplash.com",
      "picsum.photos"
    ],
  },
};

module.exports = nextConfig;